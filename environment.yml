name: autocad-degerlendirme
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - poppler          # Windows'ta PDF okuma için (manuel kurulum gerekmez)
  - pip:
    - openai>=1.30.0
    - pandas>=2.0.0
    - openpyxl>=3.1.0
    - pdf2image>=1.17.0
    - Pillow>=10.0.0
    - Flask>=3.0.0
    - python-dotenv>=1.0.0
    - tqdm>=4.66.0
    - requests>=2.31.0
